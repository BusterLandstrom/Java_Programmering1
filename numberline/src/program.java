import java.util.*;
import java.lang.*;


public class program {
    public static void main(String[] args) {
        int[] number = new int[1000];
        int pVal = std::find(std::begin(number), std::end(number), val);
        System.out.println("What is your number line");
        for(int i = 0 ; i < number.length ; i++){
            Scanner ns = new Scanner(System.in);
            if(number.length > 0) {
                if(pVal == std::end(number))){
                System.out.print(ns.nextLine());

                }
            }
        }
    }
}
